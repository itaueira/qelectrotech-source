*.snap
.flatpak-builder
# Qt build output
*.user
# doxygen Doxyfile output
doc/*
!doc/QElectroTech.qch
QElectroTech.tag
!doc/doc-utils
# Local build directories
build/
build-*/
# Python bytecode left behind by the helper scripts
__pycache__/
*.pyc
# Test sources that live outside this repository, loaded by the optional hook at the end of
# tests/catch/CMakeLists.txt. A fork drops its own tests.cmake and sources here.
tests/catch/local/
# Scratch area, local planning notes and per-user tool settings, never part of the
# repository
/tmp/
todo/
.claude/
