*.snap
.flatpak-builder
# Qt build output
*.user
# doxygen Doxyfile output
doc/*
!doc/QElectroTech.qch
QElectroTech.tag
!doc/doc-utils
# Material interno da ACME — analise de concorrencia, nao versionar
todo/_interno/
# Build local (fora do controle de versao)
build/
build-*/
# Ajuste pessoal da ferramenta de IA; o versionado e .claude/settings.json
.claude/settings.local.json

# Projetos reais da ACME usados para teste. Nunca entram no repositório: ele é público,
# e arquivo de projeto carrega nome de fornecedor e código de peça (regras 3 e 4 do CLAUDE.md).
/tmp/

# Cache do interpretador Python — os geradores em todo/exemplos/*/gerador/ deixam isto para tras
__pycache__/
*.pyc
